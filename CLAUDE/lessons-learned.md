# Lessons Learned

Backward-looking incident log. One entry per incident. Append; never delete.

## Format

```
[YYYY-MM-DD] <subsystem> — <what broke> — <why> — <rule added>
```

<!-- Append new incidents below -->
